services:
  - type: web
    name: movie-archive
    runtime: node
    buildCommand: ./Dummy/build.sh
    startCommand: ./Dummy/start.sh
    plan: free
    envVars:
      - key: MONGODB_URI
        sync: false  # You will set this manually in Render dashboard
